# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas

# Bundled Player Binaries and Libraries (built dynamically during packaging)
/resources/mpv
/resources/mpv.exe
/resources/lib/

# mpv UI assets (Anime4K shaders + ModernZ skin) are small portable text/lua
# and are committed so every build — including CI — has upscaling and the skin.
